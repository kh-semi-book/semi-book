<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="boardMapper">

	<!-- 공지사항 등록 -->
	<insert id="saveCmmPost" parameterType="CMM">
		INSERT INTO "CMM" VALUES(
		SEQ_CMM_NO.NEXTVAL,
		#{cmmTitle},#{cmmSub},#{cmmTitleImg},#{cmmConImg},DEFAULT)
	</insert>

	<!-- 프로모션 등록 -->
	<insert id="savePromotionPost" parameterType="CMM">
		INSERT INTO "PROMOTION" VALUES(
		SEQ_PROMOTION_NO.NEXTVAL, #{promotionTitle},#{promotionSub},#{promotionTitleImg},#{promotionConImg},
		#{promotionStayStartDate},#{promotionStayEndDate},#{promotionSaleStartDate},#{promotionSaleEndDate},
		#{promotionPrice},DEFAULT)
	</insert>


	<!-- 다이닝 등록 -->
	<insert id="saveDiningPost">
		INSERT INTO "DINING" VALUES(
		SEQ_DINING_NO.NEXTVAL, #{diningTitle}, #{diningSub}, #{dininigTitleImg}, #{diningConImg},
		#{diningStartDate}, #{diningEndDate}, DEFAULT)
	</insert>


	<!-- 이벤트 등록 -->
	<insert id="saveDiningPost">
		INSERT INTO "EVENT" VALUES(
		SEQ_EVENT_NO.NEXTVAL, #{eventTitle}, #{eventSub}, #{eventTitleImg}, #{eventConImg},
		#{eventStartDate}, #{eventEndDate}, DEFAULT)
	</insert>




</mapper>
